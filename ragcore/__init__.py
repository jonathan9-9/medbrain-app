"""ragcore: shared config, models, and clients used by both the ingestion
pipeline and the FastAPI backend, so retrieval/embedding logic can't drift
between what indexed the corpus and what queries it.
"""
